apiVersion: v1
kind: Service
metadata:
  name: api-node
  labels:
    {{- include "api-node.labels" . | nindent 4 }}
spec:
  selector:
    app: api-node
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
